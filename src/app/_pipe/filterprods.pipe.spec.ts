import { FilterprodsPipe } from './filterprods.pipe';

describe('FilterprodsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterprodsPipe();
    expect(pipe).toBeTruthy();
  });
});
